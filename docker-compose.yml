version: '3.8'

services:
  bot:
    build: .
    env_file: .env
    environment:
      - PYTHONPATH=/app
    depends_on:
      - redis

  redis:
    image: redis:6-alpine
    ports:
      - "6379:6379"
